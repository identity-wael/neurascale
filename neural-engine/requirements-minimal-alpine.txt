# Minimal requirements for CI builds with Alpine Linux
# Excludes packages that don't support Alpine/musl

# Core dependencies
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.0
python-dotenv==1.0.1
prometheus-client==0.21.0
redis==5.2.0
grpcio==1.65.5
protobuf==4.25.8
python-jose[cryptography]==3.4.0  # Updated to fix CVE vulnerabilities
strawberry-graphql[fastapi]==0.257.0  # Updated to fix CSRF and type resolution vulnerabilities
aiohttp==3.12.14  # For MCP server HTTP health checks

# Data Processing (minimal)
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2

# Note: TensorFlow and PyTorch are excluded as they don't support Alpine/musl
# These should be installed in a separate stage or use CPU-only versions

# GCP Services (essential)
google-cloud-storage==2.19.0
google-cloud-pubsub==2.27.1
google-cloud-logging==3.12.0

# Utilities
click==8.1.7
pyyaml==6.0.2
requests==2.32.4
