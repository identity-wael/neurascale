{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "installCommand": "pnpm config set frozen-lockfile false && pnpm install",
  "buildCommand": "cd apps/web && pnpm run build",
  "outputDirectory": "apps/web/.next"
}
